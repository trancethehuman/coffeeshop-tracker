import Card from 'react-bootstrap';

// Organize coffeeshop cards into a scrollable box
const ShopCardsContainer = () => {
    return (
        <div>
            <h3>Shop Cards Container!</h3>
        </div>
    )
}

export default ShopCardsContainer;
